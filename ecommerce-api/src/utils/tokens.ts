import jwt from "jsonwebtoken"; import { env } from "../config/env.js"; export type TokenClaims={sub:string;email:string;role:string}; export const signAccess=(claims:TokenClaims)=>jwt.sign(claims,env.JWT_ACCESS_SECRET,{expiresIn:"15m"}); export const signRefresh=(claims:TokenClaims)=>jwt.sign(claims,env.JWT_REFRESH_SECRET,{expiresIn:"30d"}); export const verifyAccess=(token:string)=>jwt.verify(token,env.JWT_ACCESS_SECRET) as TokenClaims;
