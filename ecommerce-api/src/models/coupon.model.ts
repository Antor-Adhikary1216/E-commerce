import { Schema,model } from "mongoose"; const schema=new Schema({code:{type:String,uppercase:true,trim:true,unique:true,index:true},type:{type:String,enum:["percentage","fixed"],required:true},value:{type:Number,min:0,required:true},minimumOrder:{type:Number,default:0},expiresAt:Date,isActive:{type:Boolean,default:true,index:true}},{timestamps:true});export const CouponModel=model("Coupon",schema);
