import { app } from "./app.js"; import { connectDatabase } from "./config/database.js"; import { initFirebaseAdmin } from "./config/firebase.js"; import { env } from "./config/env.js"; connectDatabase().then(()=>{initFirebaseAdmin();app.listen(env.PORT,()=>console.log(`API listening on :${env.PORT}`));}).catch(error=>{console.error("Database connection failed",error);process.exit(1);});
