import { Router } from "express"; import { z } from "zod"; import { exchangeFirebaseToken,logout,refreshAccessToken } from "../controllers/auth.controller.js"; const router=Router();router.post("/exchange",(req,res,next)=>{try{z.object({idToken:z.string().min(1)}).parse(req.body);next();}catch(e){next(e);}},exchangeFirebaseToken);router.post("/refresh",refreshAccessToken);router.post("/logout",logout);export default router;
