import { Slot } from "@radix-ui/react-slot"; import { cva, type VariantProps } from "class-variance-authority"; import { cn } from "@/lib/utils"; import { Spinner } from "./spinner";
const variants = cva("inline-flex items-center justify-center rounded-full px-5 py-3 text-sm font-semibold transition hover:scale-[1.02] disabled:opacity-50", { variants: { variant: { default: "bg-slate-950 text-white dark:bg-white dark:text-slate-950", outline: "border bg-transparent" } }, defaultVariants: { variant: "default" } });
export function Button({ className, variant, asChild, loading, disabled, children, ...props }: React.ButtonHTMLAttributes<HTMLButtonElement> & VariantProps<typeof variants> & { asChild?: boolean; loading?: boolean }) { const Comp = asChild ? Slot : "button"; return <Comp className={cn(variants({ variant }), className)} disabled={disabled || loading} {...props}>{loading ? <><Spinner size="sm" className="mr-2" />{children}</> : children}</Comp>; }